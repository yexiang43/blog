spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?characterEncoding=UTF-8
    username: root
    password: codechao
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
   root: info
   com.imcoding: debug
  file:
    name: log/imcoding-dev.log
server:
  port: 8080
