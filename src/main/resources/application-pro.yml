spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?characterEncoding=UTF-8
    username: root
    password: codechao
  jpa:
    hibernate:
     ddl-auto: update
    show-sql: true
logging:
  level:
   root: warn
   com.imcoding: info
  file:
    name: log/imcoding=pro.log
server:
  port: 8081
